Using the ticket in `ticket.txt` and the design in `design.md`, generate or refine a Python FastAPI implementation.

Requirements:
- Create a `GET /health` endpoint
- Create a `POST /summarize` endpoint
- Use Pydantic models for request and response
- Add a service layer for summarization logic
- Keep the code simple and clean
- Return structured JSON with summary, action_items, and risks

Do not overengineer the first version.
